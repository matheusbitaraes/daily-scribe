<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #fff;
        }

        .content {
            max-width: 628px;
            margin: 0 auto;
            padding: 16px 28px 16px 0;
        }

        .category {
            margin: 24px 0 12px;
            font-size: 18px;
            font-weight: bold;
            color: #000;
            padding-bottom: 0;
        }

        .main-news-section {
            margin-bottom: 32px;
        }

        .main-news-title {
            margin: 24px 0 16px;
            font-size: 20px;
            font-weight: bold;
            color: #0a97f5;
            padding-bottom: 8px;
        }

        .category-tag {
            display: inline-block;
            background-color: #f0f8ff;
            color: #0a97f5;
            font-size: 11px;
            font-weight: 600;
            padding: 2px 6px;
            border-radius: 3px;
            margin-bottom: 4px;
            text-transform: uppercase;
        }

        .category-links-section {
            margin-top: 32px;
            text-align: justify;
        }

        .category-links-title {
            margin: 24px 0 16px;
            font-size: 18px;
            font-weight: bold;
            color: #333;
            border-bottom: 2px solid #0a97f5;
            padding-bottom: 6px;
        }

        .category-link-item {
            padding: 0;
        }

        .category-link-item:last-child {
            border-bottom: none;
        }

        .category-link-item a {
            color: #0a97f5;
            text-decoration: none;
            font-weight: 500;
            font-size: 14px;
        }

        .category-link-item a:hover {
            text-decoration: underline;
        }

        .cluster-number-info {
            font-size: 10px;
            color: #666;
        }

        .cluster {
            margin-bottom: 20px;
            padding-bottom: 16px;
        }

        .title {
            font-size: 16px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 6px;
        }

        .summary {
            font-size: 15px;
            color: #444;
            margin-bottom: 10px;
            text-align: justify;
        }

        .related-list {
            margin: 6px 0 0 16px;
            padding: 0;
            list-style-type: disc;
        }

        .related-list li {
            margin-bottom: 3px;
            font-size: 12px;
            line-height: 1.4;
            text-align: justify;
        }

        .related-list a {
            color: #0a97f5;
            text-decoration: none;
            opacity: 0.8;
        }

        .related-list a:hover {
            text-decoration: underline;
            opacity: 1;
        }

        .view-more-related {
            margin: 8px 0 0 16px;
        }

        .view-more-related a {
            color: #0a97f5;
            text-decoration: none;
            font-size: 12px;
            font-weight: 600;
            opacity: 0.9;
        }

        .view-more-related a:hover {
            text-decoration: underline;
            opacity: 1;
        }

        .view-more-button {
            margin: 24px 0;
            text-align: center;
        }

        .view-more-button a {
            display: inline-block;
            background-color: #0a97f5;
            color: #fff;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 14px;
            transition: background-color 0.2s ease;
            margin: 10px;
        }

        .view-more-button a:hover {
            background-color: #0878d1;
        }

        .feedback-chip,
        .feedback-chip-small {
            display: inline-block;
            border-radius: 6px;
            text-decoration: none;
            line-height: 0;
            vertical-align: top;
        }

        .feedback-chip {
            padding: 6px 8px;
        }

        .feedback-chip-small {
            padding: 4px 6px;
        }

        .feedback-chip-tiny {
            display: inline-block;
            border-radius: 4px;
            padding: 2px 4px;
            text-decoration: none;
            line-height: 0;
            vertical-align: middle;
        }

        .feedback-chip:hover,
        .feedback-chip-small:hover {
            background-color: #d0e9ff;
        }

        .feedback-chip:hover.feedback-chip-negative,
        .feedback-chip-small:hover.feedback-chip-negative {
            background-color: #e8ebf3;
        }

        .feedback-hint {
            font-size: 11px;
            color: #9ca3af;
        }

        .feedback-chip img,
        .feedback-chip-small img {
            display: block;
        }

        .align-top {
            vertical-align: top;
        }

        .vote-cell {
            width: 28px;
        }

        .category-vote-cell {
            width: 48px;
            white-space: nowrap;
        }

        .category-link {
            margin-bottom: 6px;
        }

        /* Mobile optimizations */
        @media (max-width: 600px) {
            .related-list {
                margin: 4px 0 0 14px;
            }

            .related-list li {
                margin-bottom: 2px;
                font-size: 11px;
                line-height: 1.3;
            }
        }

        .header-table {
            width: 100%;
            margin-bottom: 20px;
        }

        .header-title-cell,
        .header-links-cell {
            text-align: center;
            vertical-align: middle;
        }

        .header-logo {
            margin: 0 auto;
            display: block;
        }

        .header-links-cell {
            padding-top: 8px;
            white-space: nowrap;
            font-size: 12px;
            color: #6b7280;
        }

        .header-link {
            color: #6b7280;
            text-decoration: none;
            font-weight: 500;
        }

        .header-link:hover {
            text-decoration: underline;
        }

        .header-link-separator {
            display: inline-block;
            margin: 0 12px;
            color: #9ca3af;
            font-weight: 400;
        }

        .sub-header {
            max-width: 600px;
            margin: 0 auto 16px;
            padding: 0 16px;
            text-align: center;
        }

        .main-article {
            display: inline-block;
        }
    </style>
</head>

<body>
    <div class="header">
        <table class="header-table" role="presentation" cellpadding="0" cellspacing="0">
            <tbody>
                <tr>
                    <td class="header-title-cell">
                        <img class="header-logo" src="https://dailyscribe.news/daily-scribe-title-no-bg.png" alt="Daily Scribe Logo"
                            width="200">
                    </td>
                </tr>
                <tr>
                    <td class="header-links-cell">
                        <a class="header-link" href="{{base_url}}/news" target="_blank" rel="noopener noreferrer">Ver todas as notícias</a>
                        {{#if preference_token}}
                        <span class="header-link-separator">|</span>
                        <a class="header-link" href="{{base_url}}/preferences/{{preference_token}}" target="_blank" rel="noopener noreferrer">Gerenciar preferências</a>
                        {{/if}}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="sub-header">
        <div class="feedback-hint"> Use <span title="{{feedback.tooltip_positive}}">
            <img class="feedback-chip-tiny feedback-chip-positive"
    src="{{base_url}}/keyboard_arrow_up_24dp_000000.png" alt="" width="10"></span>
            <span title="{{feedback.tooltip_negative}}">
            <img class="feedback-chip-tiny feedback-chip-negative"
    src="{{base_url}}/keyboard_arrow_down_24dp_000000.png" alt="" width="10"></span> e treine o algoritmo para entregar as melhores noticias para você.
        </div>
    </div>
    <div class="content">
        {{#if main_news}}
        <div class="main-news-section">

            {{#each main_news}}
            <div class="cluster">
                <table>
                    <tbody>
                        <td class="align-top vote-cell">
                            <a class="feedback-chip feedback-chip-positive" href="{{feedback.positive_url}}" target="_blank" rel="noopener noreferrer"
                                title="{{feedback.tooltip_positive}}" aria-label="{{feedback.tooltip_positive}}"><img
                                    src="{{../base_url}}/keyboard_arrow_up_24dp_000000.png" alt="" width="12"></a><a class="feedback-chip feedback-chip-negative" href="{{feedback.negative_url}}" target="_blank" rel="noopener noreferrer"
                                title="{{feedback.tooltip_negative}}" aria-label="{{feedback.tooltip_negative}}"><img
                                    src="{{../base_url}}/keyboard_arrow_down_24dp_000000.png" alt="" width="12"></a>
                        </td>
                        <td class="summary align-top">
                            <span class="title">{{main_article.preferred_title}}:</span>
                            {{main_article.preferred_summary}} <a
                                href="{{main_article.wrapped_url}}">[{{main_article.source}}]</a>

                            {{#if related_articles}}
                            <ul class="related-list">
                                {{#each related_articles}}
                                <li>
                                    <a href="{{wrapped_url}}">{{display_title}}</a>
                                </li>
                                {{/each}}
                            </ul>
                            {{#if has_more_related}}
                            <div class="view-more-related">
                                <a href="{{../base_url}}/news/{{main_article.article_id}}">
                                    + {{additional_related_count}} notícias relacionadas - veja mais
                                </a>
                            </div>
                            {{/if}}
                            {{/if}}
                        </td>

                    </tbody>
                </table>


            </div>
            {{/each}}
        </div>
        {{/if}}

        {{#if category_links}}
        <div class="category-links-section">
            {{#each category_links}}
            <div class="category">Outras em {{category_name}}</div>
            {{#each articles}}
            <div class="category-link">
                <table>
                    <tbody>
                        <tr>
                            <td class="align-top category-vote-cell">
                                <a class="feedback-chip-small feedback-chip-positive" href="{{feedback.positive_url}}" target="_blank" rel="noopener noreferrer"
                                    title="{{feedback.tooltip_positive}}" aria-label="{{feedback.tooltip_positive}}"><img
                                        src="{{../../base_url}}/keyboard_arrow_up_24dp_000000.png" alt="" width="10"></a><a class="feedback-chip-small feedback-chip-negative" href="{{feedback.negative_url}}" target="_blank" rel="noopener noreferrer"
                                    title="{{feedback.tooltip_negative}}" aria-label="{{feedback.tooltip_negative}}"><img
                                        src="{{../../base_url}}/keyboard_arrow_down_24dp_000000.png" alt=""
                                        width="10"></a>
                            </td>
                            <td class="category-link-item align-top">
                                <a href="{{../../base_url}}/news/{{article_id}}">{{preferred_title}}</a>
                                {{#if similar_news_count}}
                                <span class="cluster-number-info">[+{{similar_news_count}} similares]</span>
                                {{/if}}
                            </td>
                        </tr>
                    </tbody>

                </table>
            </div>
            {{/each}}
            {{/each}}
        </div>
        {{/if}}
        {{#if unsubscribe_link_html}}
        {{{unsubscribe_link_html}}}
        {{/if}}
    </div>

</body>

</html>